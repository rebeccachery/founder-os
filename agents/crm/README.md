# CRM Agent

Manages contacts and follow-up reminders. v1 is a stub — extend to import leads from search or email.

## Run locally

```bash
python workflows/run_agent.py --agent crm
```

## Output

- Table: `contacts`
- Dashboard: follow-ups appear in `/deadlines`
